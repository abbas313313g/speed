

'use server';

/**
 * @fileOverview This file is intentionally left blank. 
 * The AI support logic has been removed as per the new implementation where admins directly chat with users.
 * The functions `askAiSupport`, `AiSupportInput`, `AiSupportOutput` are no longer used.
 */
