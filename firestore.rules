rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all collections.
    // This is for demonstration purposes after removing auth.
    // In a real production app, you would have more granular rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
